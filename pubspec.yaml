name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  audioplayers: ^6.0.0
  sqflite: ^2.3.0
  path: ^1.8.3
  cupertino_icons: ^1.0.8
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1
  firebase_messaging: ^16.0.1
  flutter_local_notifications: ^19.4.1
  image_picker: ^1.2.0
  http: ^1.5.0
  camera: ^0.10.5+9
  video_player: ^2.8.2  # ✅ أضيفي هاد
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  

flutter:
  assets:
    - assets/images/
    - assets/videos/  # ✅ أضيفي هاد

  uses-material-design: true